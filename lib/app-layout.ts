/**
 * Максимальная ширина колонки приложения (согласовано с `.app-shell` в `app/globals.css`).
 * Используется в модалках, чтобы на ПК совпадать с основной сеткой.
 */
export const APP_SHELL_MAX =
  "max-w-lg md:max-w-3xl lg:max-w-4xl xl:max-w-5xl 2xl:max-w-6xl" as const;
